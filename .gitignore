# Environment & secrets — .env.example is safe to commit (no real keys)
.env
.env.local
.env.*.local

# Large raw data files — regenerate via ingestion scripts
data/fundamentals/
data/prices/
data/analyst/
data/sectorscope.duckdb

# Old backtest results (superseded)
data/results/archive/

# Parquet result files (large, regenerate from scripts)
data/results/*.parquet

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# IDE
.idea/
.vscode/

# Jupyter checkpoints
.ipynb_checkpoints/
